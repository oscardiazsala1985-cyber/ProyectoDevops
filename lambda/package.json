{
  "name": "sre-process-service",
  "version": "1.0.0",
  "description": "SRE Process Service — Lite Thinking 2026",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --forceExit --coverage",
    "lint": "eslint *.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  }
}
